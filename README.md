# 預金差押予定一覧 作成ツール（deposit_seizure_list）GUI版 機能要件書

## 1. ツールの目的

pipitLINQ経由で取得した電子預金照会結果（.xlsm）から、差押対象口座の情報を職員が対話的に選択し、WinActorで読み込み可能なCSV（預金差押予定一覧.csv）を生成する。同時に、差押起案の印刷・証跡用として照会結果ファイルを文書番号付きで保存する。現在職員が手作業で行っている口座選択・差押文言作成・一覧転記・ファイル整理を半自動化し、ミスの削減と処理速度の向上を実現する。

本書はWPF GUI版（.cs/.exe）の機能要件を記述する。ビジネスロジック（Excel読取・差押文言生成・CSV出力・印刷ファイル保存等）はコンソール版（.ps1）と同等であり、コンソール版の要件書も併せて参照のこと。

---

## 2. 技術構成

### 2.1 ビルド環境
- .NET Framework 4.0 / csc.exe による単一ファイルコンパイル。
- WPF（PresentationFramework, PresentationCore, WindowsBase, System.Xaml）を使用。
- XAML はインライン文字列として C# 内に埋め込み、XamlReader.Load で動的にパースする。
- ビルドは deposit_seizure_list_build.bat を実行して deposit_seizure_list.exe を生成する。

### 2.2 コード構成（単一 .cs ファイル）
| クラス | 役割 |
|--------|------|
| ProfileConfig / AppConfig | JSON設定のデシリアライズモデル |
| EraEntry | 元号マッピングのデシリアライズモデル |
| AccountItem | 口座テーブル1行のデータモデル（ListViewバインディング対象） |
| FileEntry | 処理キュー内のファイル状態管理（Pending / Added / Skipped） |
| JsonHelper | .NET 4.0 の DataContractJsonSerializer ラッパー |
| ConfigLoader | 設定ファイル・マッピングJSON群のロード |
| BusinessLogic | データ加工・バリデーション・文言生成等のビジネスロジック |
| DepositSeizureApp | WPF Application 本体（UI構築・イベント制御・非同期処理） |
| RelayCommand | ICommand 実装（キーバインディング用） |

### 2.3 カラーパレット
file_search（青 #005FB8）との視覚的な差別化のため、ティール系を採用。

| 用途 | カラーコード |
|------|------------|
| プライマリアクセント（ヘッダー・ボタン・ラベル・スピナー） | #00897B |
| ボタンホバー | #00796B |
| 口座テーブル選択行 | #E0F2F1 |
| 口座テーブルホバー行 | #F1F9F8 |
| ゴーストボタンホバー | #EEF6F5 |
| 初期画面D&D枠線 | #B2CECB |
| 成功アイコン | #107C41（背景 #E8F5ED） |
| エラーアイコン | #D32F2F（背景 #FCEBEB） |
| スキップアイコン | #00897B（背景 #E0F2F1） |

### 2.4 タイポグラフィ

#### フォントサイズ階層
| サイズ | 用途 |
|--------|------|
| 36px | 初期画面フォルダアイコン |
| 32px | 結果カードアイコン（✓/✗/→） |
| 17px | 結果カードタイトル |
| 14px | 初期画面D&D案内テキスト |
| 13px | ヘッダーバータイトル、セクションヘッダー（基本情報・口座選択）、プライマリボタン |
| 12px | 入力値（TextBox / ComboBox）、口座テーブルデータ、ゴーストボタン、チェックボックスラベル、結果カードサブテキスト |
| 11px | フィールドラベル、テーブルヘッダー、「シート:」ラベル、ファイルリンク、フッター、ガイドテキスト、結果カード件数テキスト |
| 10px | 届出住所バリデーションエラー |

#### 文字色
| 色 | 用途 |
|-----|------|
| White | ヘッダーバータイトル、プライマリボタンテキスト |
| #333 | 入力値（TextBox / ComboBox / CheckBox）、口座テーブルデータ |
| #555 | 「シート:」ラベル、ゴーストボタンテキスト |
| #666 | フッター、初期画面D&D案内テキスト |
| #777 | フィールドラベル、テーブルヘッダー |
| #999 | プライマリボタン無効時、結果カードサブテキスト（初期値）、結果カード件数テキスト |
| #00897B | セクションヘッダー、ファイルリンク、結果カードサブテキスト（成功時） |
| #D32F2F | バリデーションエラー、ガイドテキスト、結果カードサブテキスト（エラー時） |

---

## 3. 起動方式

### 3.1 直接起動
- deposit_seizure_list.exe をダブルクリックで起動する。
- 初期画面（ドラッグ＆ドロップゾーン）が表示される。

### 3.2 ドラッグ＆ドロップ起動
- .xlsm ファイルを exe またはショートカットにD&Dして起動する。
- 複数ファイルを一度に処理可能（順次処理）。

### 3.3 コマンドライン引数起動
- file_search.exe の「差押リストに追加」から呼び出される。
- 引数で渡されたファイルパスを処理キューに追加する。
- `isFromFileSearch = true` となり、全ファイル処理完了後にアプリを自動クローズする。

---

## 4. 画面構成

### 4.1 ウィンドウ
- タイトル：「預金差押予定一覧 作成ツール」
- サイズ：1000×700（最小 900×520）
- 3つの画面状態（InitialPanel / MainPanel / OverlayPanel）を重ね合わせた Grid で構成する。

### 4.2 初期画面（InitialPanel）
- 中央にドラッグ＆ドロップゾーン（破線枠 + フォルダアイコン）を配置する。
- 「ファイルを選択」ボタンでファイル選択ダイアログも使用可能。
- ウィンドウ全体へのD&Dに対応し、.xlsm ファイルのみ受け付ける。

### 4.3 メインフォーム（MainPanel）
- ヘッダーバー（ティール、ツール名表示）
- シート選択 ComboBox + ファイルリンク + 再読み込みボタン
- 基本情報セクション（宛名番号・氏名・金融機関・処分担当・住民票住所・届出住所・執行日）
- 口座選択セクション（ListView + GridView）
- フッターバー（件数表示 + 出力先パス）
- アクションボタン群（ファイルを読み込む / スキップ / 一覧に追加）

### 4.4 オーバーレイ（OverlayPanel）
- 半透明白背景（#CCFFFFFF）でメインフォーム全体を覆う。
- ローディングオーバーレイ：回転スピナー（青弧の Path + RotateTransform）
- 結果オーバーレイ：処理結果カード

---

## 5. 基本情報セクション

### 5.1 読取専用フィールド
- 宛名番号（10桁0埋め、Consolas フォント、グレー背景）
- 金融機関名

### 5.2 編集可能フィールド
- 氏名、処分担当、住民票住所（✎マーク付きラベル）
- 届出住所（チェックボックス連動、50文字制限バリデーション付き）
- Excel から読み取った値が初期表示されるが、ユーザーが TextBox 上で直接書き換えることができる。「一覧に追加」押下時に TextBox の現在値が CSV に出力されるため、読取値の手動補正が可能。コンソール版にはこの機能はない。

### 5.3 執行日入力
- テキスト入力（Consolas フォント）+ カレンダーPopupボタン。
- 7桁和暦（例: 5080615）または西暦（例: 2026/06/15）を受け付ける。
- LostFocus 時に自動パース・バリデーションを実行し、不正値は赤枠で警告する。

### 5.4 カレンダーPopup
- Popup + Calendar コントロール（SelectionMode: SingleDate）。
- 日付選択時に自動で Popup を閉じ、入力欄に反映する。
- Popup 展開時に DisplayMode を Month にリセットし、前回の年/月選択ビューが残らないようにする。
- SelectedDates をクリアしてから開くことで、同じ日付の再クリックでも SelectedDatesChanged が発火する。

---

## 6. 口座選択セクション

### 6.1 口座テーブル（ListView + GridView）
- 6列：支店名 / 支店番号 / 口座種別 / 最終取引日（満期日）/ 口座番号 / 残高
- 口座番号に差押実績がある場合は「★」マークを付与し、ToolTip に執行日と文書番号を表示する（例:「差押実績あり（執行日: 2026年6月15日 / 文書番号: 10103）」）。
- 選択行はティール背景（#E0F2F1）＋左端に3pxアクセントバーで強調する。
- ウィンドウリサイズ時に列幅を自動調整する（SizeChanged イベント）。

### 6.2 ガイドテキスト
- 口座が0件の場合：「表示可能な口座がありません」
- 口座未選択の場合：「口座を選択してください」
- 執行日未入力の場合：「執行日を入力してください」
- すべて満たされた場合：ガイドテキスト非表示、「一覧に追加」ボタン有効化

---

## 7. 非同期処理

### 7.1 BackgroundWorker パターン
- Excel COM 操作はすべて BackgroundWorker で非同期実行する。
- UI スレッドのブロックを防ぎ、処理中はローディングオーバーレイ（スピナー）を表示する。

### 7.2 非同期処理の発生箇所
| 処理 | トリガー | BackgroundWorker の内容 |
|------|---------|----------------------|
| ファイル読込 | D&D / ファイル選択 / 次のファイルへ | ReadExcelFile（COM でファイルオープン→シートデータ読取） |
| シート切替 | ComboBox 選択変更 | ReadSheetData（選択シートのデータ再読取） |
| 一覧に追加 | ボタン押下 | ProcessAdd（文書番号採番→差押文言生成→CSV追記→印刷ファイル保存） |

### 7.3 冗長読込の抑止
- PopulateForm 内で sheetCombo.SelectedIndex を設定する際、suppressSheetChange フラグで SelectionChanged を抑止する。
- ReadExcelFile で初回シートのデータを既に取得済みのため、二重の COM アクセスを回避する。

### 7.4 Excel COM 一括取得（Value2 バルクリード）
- 表紙列スキャンおよび口座テーブル読取りでは、`Range.Value2` による範囲一括取得で COM 呼出し回数を最小化する。
- 表紙列: `ws.Range[coverCol+"1", coverCol+lastUsedRow].Value2` で列全体を1回の COM 呼出しで取得し、2D配列を走査して coverValue / stopValues を検出する。
- 口座テーブル: 表紙ブロックごとに `ws.Range[leftCol+startRow, rightCol+upperLimit].Value2` で矩形範囲を1回の COM 呼出しで取得し、`BulkCell` / `BulkCellRaw` ヘルパーで各列にアクセスする。
- 列インデックスは `ColToIndex` で事前計算し、一括取得データの列オフセットに変換する。
- Value2 が単一値（1行1列の場合にスカラー値を返す）のケースにも対応する。

---

## 8. アニメーション

### 8.1 スピナー回転
- ローディングオーバーレイ中央に270°の青弧（Path + ArcSegment）を表示する。
- RotateTransform + DoubleAnimation（By=360, 1秒, Forever）で無限回転する。
- loadingOverlay の IsVisibleChanged イベントで自動的に開始/停止し、非表示時のリソース消費を防止する。

### 8.2 オーバーレイフェード
| メソッド | 動作 | 時間 |
|----------|------|------|
| FadeInOverlay | overlayPanel の Opacity 0→1 | 150ms |
| FadeOutOverlay | overlayPanel の Opacity 1→0 → Collapsed | 150ms |
| CrossFadeToResult | overlayPanel を一瞬フェードアウト → コンテンツ差替 → フェードイン | 100ms × 2 |

### 8.3 フェードの使い分け
| 場面 | 方式 |
|------|------|
| ローディング開始 | FadeInOverlay |
| ファイル読込成功 / シート切替完了 | FadeOutOverlay |
| ローディング → 結果カード | CrossFadeToResult |
| スキップ（メインフォームから直接結果表示） | ShowResult + FadeInOverlay |
| 次のファイルへ / 完了 | FadeOutOverlay（コールバック付き） |
| 完了 → 初期画面 | FadeOutOverlay → mainPanel FadeOut → initialPanel FadeIn（150ms×3段） |

---

## 9. 結果カード

### 9.1 構造
2層構造（タイトル + サブテキスト1行）で統一する。

| タイプ | アイコン | アイコン背景 | タイトル | サブテキスト | サブテキスト色 |
|--------|---------|------------|---------|------------|-------------|
| success | ✓ | #E8F5ED | 一覧に追加しました | 文書番号: XXXXX | #00897B（ティール） |
| skip | → | #E0F2F1 | スキップしました | ファイル名 | #999（グレー） |
| error | ✗ | #FCEBEB | 処理失敗 / 読込失敗 | エラーメッセージ | #D32F2F（赤） |

### 9.2 アクションボタン
- 次のファイルがある場合：「次のファイルへ →」＋ 件数サブテキスト（「X / Y 件目へ進みます」）
- 最終ファイルまたは単一ファイルの場合：「完了」のみ（サブテキスト非表示）

### 9.3 完了時の動作
- file_search からの起動：アプリをクローズ（Shutdown）
- 直接起動：初期画面に戻る

---

## 10. フォーカス制御

### 10.1 空白クリック
- ウィンドウの背景領域（Panel / Border / Window）をクリックすると、テキストボックスからフォーカスを外す。

### 10.2 口座テーブル空白エリア
- ListView のアイテムが無い空白部分をクリックした場合も、フォーカスを外す。
- VisualTree を辿って ListViewItem が見つからなければ空白エリアと判定する。

---

## 11. ショートカットキー

| キー | 動作 |
|------|------|
| Escape | 結果カード表示中に「次のファイルへ」と同じ動作 |
| Ctrl+O | ファイル選択ダイアログを開く |

---

## 12. file_search.exe との連携

### 12.1 呼び出し方式
- file_search.cs の AddToSeizureList メソッドが deposit_seizure_list.exe を子プロセスとして起動する。
- 引数にファイルパスを渡す（複数ファイルの場合はスペース区切り）。
- UseShellExecute = true で直接起動（PowerShell 不要）。

### 12.2 設定
- file_search_config.json の depositSeizureListScript に .exe のパスを設定する。

---

## 13. データ加工処理

コンソール版と同等のビジネスロジックを BusinessLogic クラスに集約している。

### 13.1 ゆうちょ銀行対応
- yucho_center_mapping.json により、支店番号（記号）の2〜3桁目から貯金事務センター名を自動補完する。
- 口座カテゴリ（総合口座 / 振替口座）により参照テーブルを切り替える。

### 13.2 支店名「支店」サフィックス
- ゆうちょ以外の金融機関で、支店名の末尾に「支店」がない場合に自動付与する。
- 末尾が「営業部」「出張所」「公務部」「本店」の場合は付与しない。

### 13.3 金融機関名・口座種別マッピング
- institution_name_mapping.json により金融機関名を補正する。
- account_type_mapping.json により口座種別を補完する（global + 金融機関別の2階層）。
- branch_name_mapping.json により支店名を補正する。
- いずれも任意ファイルで、存在しない場合は補正なし。

### 13.4 全角変換
- 金融機関名のマッピング比較時に半角→全角正規化（ToFullWidth）を行い、表記ゆれを吸収する。

---

## 14. 文書番号の採番

### 14.1 排他ロック
- document_number_counter.json を FileStream 排他ロック（FileShare.None）で開き、「読み取り→採番→書き戻し」をアトミックに実行する。
- 複数職員が同時使用した場合の重複採番を防止する。

### 14.2 リトライ
- ロック取得に失敗した場合は最大5回、500ms間隔でリトライする。

### 14.3 CSV書き込み失敗時のロールバック
- CSV書き込みが全リトライ失敗した場合、消費済みの文書番号カウンターを1つ戻す。

---

## 15. 差押文言の生成

### 15.1 差押文言の構成
- 3つのフィールド（差押文言1〜3）に分割して出力する。
- 文言は「上記滞納者が、債務者である〜」形式で構成する。
- 金融機関名には「扱」を付与する。

### 15.2 ゆうちょ銀行の特殊形式
- ゆうちょ銀行の場合は「記号番号：支店番号－口座番号」形式。
- その他は「口座番号：口座番号」形式。

### 15.3 振込手数料
- transfer_fee_mapping.json で金融機関ごとの振込手数料文言を差押文言2に出力する。

---

## 16. CSV出力

### 16.1 出力ファイル
- ファイル名：「預金差押予定一覧.csv」（config の outputFolder 配下）。
- エンコーディング：BOM付きUTF-8。

### 16.2 追記方式
- 既存CSVに1行ずつ追記する。
- 排他ロック（FileShare.None）で書き込み、リトライ機構（5回・500ms間隔）を備える。

### 16.3 CSV列構成（20列）

| # | 列名 | 説明 |
|---|------|------|
| 1 | 登録日時 | CSV書き込み日時（yyyy/MM/dd HH:mm形式） |
| 2 | 宛名番号 | 10桁0埋め |
| 3 | 氏名 | |
| 4 | 職員名 | |
| 5 | 執行日 | 7桁和暦（元号コード＋年月日） |
| 6 | 住民票住所 | |
| 7 | 銀行届出住所 | チェックボックスON時のみ出力 |
| 8 | 金融機関名 | マッピング補正後 |
| 9 | 支店名 | サフィックス付与・ゆうちょ変換後 |
| 10 | 支店番号 | |
| 11 | 口座種別 | マッピング補完後 |
| 12 | 口座番号 | |
| 13 | 差押文言1 | |
| 14 | 差押文言2 | |
| 15 | 差押文言3 | |
| 16 | 文書番号 | 排他採番された番号 |
| 17 | 照会結果ファイル名 | 保存先のファイル名（文書番号.xlsm） |
| 18 | 処理済フラグ1 | WinActor用（空で出力） |
| 19 | 処理済フラグ2 | WinActor用（空で出力） |
| 20 | 処理済フラグ3 | WinActor用（空で出力） |

---

## 17. 照会結果ファイルの保存

### 17.1 保存先
- printFolder（config で設定）配下に「文書番号.xlsm」として保存する。

### 17.2 不要データの削除
- 選択シート以外のシートを削除する（VeryHidden シートは保持）。
- 選択した口座に対応しない表紙ブロック（カバーページ＋口座テーブル）を削除する。
- 明細ページの削除：detailAccountNumberCell / detailAccountTypeCell で口座を照合し、選択口座以外の明細ページを削除する。

---

## 18. 設定ファイル一覧

### 18.1 必須ファイル
| ファイル名 | 説明 |
|-----------|------|
| deposit_seizure_list.cs | ソースコード |
| deposit_seizure_list_build.bat | ビルドスクリプト |
| deposit_seizure_list_config.json | プロファイル設定（セルマッピング・出力先等） |
| document_number_counter.json | 文書番号カウンター（nextNumber） |
| era_mapping.json | 元号マッピング（和暦⇔西暦変換） |

### 18.2 任意ファイル（なくても動作するが機能が制限される）
| ファイル名 | 説明 |
|-----------|------|
| yucho_center_mapping.json | ゆうちょ貯金事務センター名の自動補完 |
| account_type_mapping.json | 口座種別の自動補完 |
| institution_name_mapping.json | 金融機関名の補正 |
| branch_name_mapping.json | 支店名の補正 |
| transfer_fee_mapping.json | 振込手数料の補正 |

---

## 19. config.json の主要設定項目

### 19.1 プロファイル共通
| 項目 | 必須 | 説明 |
|------|------|------|
| name | ○ | プロファイル名 |
| outputFolder | ○ | CSV出力先フォルダ |
| printFolder | ○ | 照会結果保存先フォルダ |
| defaultFolder | | GUI版のファイル選択ダイアログ初期フォルダ |

### 19.2 セルマッピング
| 項目 | 説明 | 設定例 |
|------|------|--------|
| coverCell | 回答書表紙の検出セル列 | A2 |
| coverValue | 回答書表紙の検出値 | 回答書（単票） |
| filterCell | シートフィルタリングのセル位置 | T6 |
| filterValue | シートフィルタリングの検索値（文字列または配列） | ["現年", "滞繰"] |
| addressNumberCell | 宛名番号のセル位置 | X11 |
| staffCell | 職員名のセル位置 | H6 |
| nameCell | 氏名のセル位置 | H14 |
| residenceAddressCell | 住民票住所のセル位置 | H15 |
| deliveryAddressCell | 届出住所のセル位置 | Y15 |
| financialInstitutionCell | 金融機関名のセル位置 | Q9 |

### 19.3 口座テーブル列
| 項目 | 説明 | 設定例 |
|------|------|--------|
| branchNameCol | 支店名の列 | C |
| branchNumberCol | 支店番号の列 | J |
| accountTypeCol | 口座種別の列 | L |
| lastTransactionCol | 最終取引日の列 | Q |
| accountNumberCol | 口座番号の列 | U |
| balanceCol | 残高の列 | Z |
| accountStartRow | 口座テーブル開始行 | 26 |

### 19.4 明細ページ設定（任意）
| 項目 | 説明 |
|------|------|
| stopValues | 明細ページ検出用の値（配列） |
| detailAccountNumberCell | 明細ページの口座番号セル位置（stopValues セルからの相対座標） |
| detailAccountTypeCell | 明細ページの口座種別セル位置（stopValues セルからの相対座標） |

---

## 20. コンソール版との主な差異

| 項目 | コンソール版（.ps1） | GUI版（.cs/.exe） |
|------|-------------------|------------------|
| 実行環境 | PowerShell | .NET Framework 4.0 WPF |
| 起動方法 | bat → PowerShell | bat → csc.exe → exe |
| ユーザー操作 | テキスト入力（番号選択・Y/N/R） | GUI操作（クリック・D&D・カレンダー） |
| シート選択 | 番号入力 | ComboBox |
| 口座選択 | 番号入力 | ListView クリック |
| 執行日入力 | テキスト入力（元号/西暦） | テキスト入力 + カレンダーPopup |
| 確認画面 | Y/N/R の3択 | 「一覧に追加」/「スキップ」ボタン |
| 処理中表示 | なし（同期実行） | スピナー + フェードアニメーション |
| ログ | トランスクリプトファイル出力 | なし（結果カードで即時フィードバック） |
| サマリー | 処理完了時に一覧表示 | なし（1件ずつ結果カードで表示） |
| file_search 連携 | PowerShell 経由 + 環境変数 | exe 直接起動（引数にパスを渡す） |


# deposit_seizure_list 機能比較表（コンソール版 .ps1 vs GUI版 .cs）

## 凡例
- ✅ 同等の実装
- ⚠️ 動作は同等だが実装方式が異なる
- 🔶 意図的な仕様変更（GUI版で設計判断として変更）

---

## 1. アーキテクチャ

| 項目 | コンソール版（.ps1） | GUI版（.cs） | 状態 |
|------|---------------------|-------------|------|
| 実行環境 | PowerShell 5.0 | .NET Framework 4.0 WPF | ⚠️ |
| COM バインディング | PSランタイムの動的バインディング | `dynamic` late binding（Microsoft.CSharp） | ⚠️ |
| COM解放 | ループ解放（`while ReleaseComObject`） | 1回解放（PSランタイムの暗黙参照カウント増加がC#では発生しないため） | ⚠️ 正当 |
| 非同期処理 | なし（同期実行） | BackgroundWorker（UIブロック防止） | 🔶 |
| Excel インスタンス | 処理完了後に毎回Quit | アプリ生存期間中保持（ファイルごとの起動/終了を行わない） | 🔶 |

---

## 2. 起動・ファイル入力

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| バッチ D&D 起動 | ✓ | ✓（exe に D&D） | ✅ |
| 複数ファイル順次処理 | ✓ | ✓ | ✅ |
| ファイル選択ダイアログ | なし | ✓（初期画面 + メインフォーム） | 🔶 |
| ウィンドウ内 D&D | なし | ✓（全画面状態で受付） | 🔶 |
| file_search 連携 | PowerShell 経由 + 環境変数 | exe 直接起動（引数にパス） | ⚠️ |
| ファイルロック待ち | ✓（`Wait-ForFileUnlock`） | なし | 🔶 不要※1 |

> ※1: コンソール版の `Wait-ForFileUnlock` は読取専用オープンの前に配置されており、読取専用では排他ロックを要求しないため実質的に不要。GUI版でロック待ちを実装しないのは正しい判断。

---

## 3. シート選択・フィルタリング

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| filterCell/filterValue フィルタ | ✓（部分一致、OR条件） | ✓（部分一致、OR条件） | ✅ |
| filterValue 配列対応 | ✓ | ✓ | ✅ |
| フィルタなし時のシート収集 | `Visible -ne VeryHidden`（定数バグで実質全シート） | `Visible == -1`（Visible のみ） | ⚠️ ※2 |
| フィルタあり時のVeryHidden除外 | スキャン対象を制限（定数バグで機能せず） | 全シートスキャン（VeryHiddenはフィルタ不一致で除外） | ⚠️ ※2 |
| シートが1件の場合 | 自動選択 | ComboBox に1件表示（自動選択相当） | ✅ |
| シートが0件の場合 | 警告 → ファイルスキップ | メインフォーム上にメッセージ表示 | 🔶 |
| シート切替 | なし（選択後は固定） | ComboBox で動的切替可能 | 🔶 |

> ※2: コンソール版の `$xlSheetVeryHidden = -2` は Excel の実定数（`2`）と異なるため、VeryHidden 除外が機能していない。GUI版は正しい定数値（`2`）を使用。

---

## 4. 回答書表紙の検出

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| coverCell 列のスキャン | 範囲一括取得（`Range.Value2`） | 範囲一括取得（`Range.Value2`） | ✅ |
| coverValue 完全一致 | ✓ | ✓ | ✅ |
| stopValues 同時検出 | ✓ | ✓ | ✅ |
| 複数表紙対応 | ✓（オフセット方式） | ✓（オフセット方式） | ✅ |
| 表紙が0件の場合 | 警告 → ファイルスキップ | オフセット0で続行 | 🔶 ※3 |

> ※3: コンソール版は表紙未検出をエラーとして扱うが、GUI版はオフセット0で処理を続行する。実運用上、excel_sheet_extract で事前に弾かれるためこのツールに表紙なしファイルが渡されることはない。

---

## 5. 基本情報の取得

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 宛名番号（10桁0埋め） | ✓ | ✓ | ✅ |
| 氏名 | ✓ | ✓ | ✅ |
| 職員名 | ✓ | ✓ | ✅ |
| 住民票住所（郵便番号除去・空白除去） | ✓ | ✓ | ✅ |
| 届出住所 | ✓ | ✓ | ✅ |
| 金融機関名 + institution_name_mapping 補正 | ✓（全角正規化比較） | ✓（全角正規化比較） | ✅ |
| オフセット加算 | ✓（Get-OffsetCell） | ✓（BusinessLogic.GetOffsetCell） | ✅ |
| フィールドの自由編集 | なし（読取値をそのまま出力） | ✓（✎マーク付きTextBoxで直接編集可能） | 🔶 ※4 |

> ※4: GUI版では氏名・処分担当・住民票住所の各フィールドが編集可能なTextBoxとして表示される。「一覧に追加」押下時にTextBoxの現在値がCSVに出力されるため、Excel読取値を手動補正してから登録できる。コンソール版にはこの機能はない。

---

## 6. 口座テーブルの読み取り

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 読取り方式 | 範囲一括取得（`Range.Value2` → 2D配列） | 範囲一括取得（`Range.Value2` → 2D配列） | ✅ |
| accountStartRow | ✓ | ✓ | ✅ |
| 口座番号列で終端判定 | ✓（空行チェック → 非数値チェックの2段階） | ✓（口座番号空 → スキップ、非数値チェック） | ⚠️ ※5 |
| 複数ページ結合 | ✓ | ✓ | ✅ |
| nextCoverRow / stopRow 越境防止 | ✓ | ✓ | ✅ |
| 行範囲上限の事前算出 | ✓（nextCoverRow, stopRow, MAX_ROWS の最小値） | ✓（同一ロジック） | ✅ |
| ACCOUNT_TABLE_MAX_ROWS | 200 | 200 | ✅ |
| 支店名の空白除去 | ✓（半角・全角） | ✓（半角・全角） | ✅ |
| ゆうちょ事務センター自動補完 | ✓ | ✓ | ✅ |
| ゆうちょ事務センター手入力フォールバック | ✓（対話的に入力） | なし（空欄口座は非表示） | 🔶 |
| 支店名が空の口座 | 表示（選択後に手入力） | 非表示（テーブルに表示しない） | 🔶 |
| 支店名「支店」サフィックス | ✓（営業部・出張所を除外） | ✓（営業部・出張所・公務部・本店を除外） | ⚠️ ※6 |
| 最終取引日（シリアル値/文字列対応） | ✓ | ✓ | ✅ |
| 残高取得 | ✓ | ✓ | ✅ |
| 各表紙ブロックの届出住所取得 | ✓ | ✓ | ✅ |

> ※5: コンソール版は「全フィールドが空の行」を先にスキップし、次に「口座番号・支店番号がともに非数値」をスキップする2段階。GUI版は口座番号が空なら即スキップ→非数値チェック。実データではほぼ同等の結果。
> ※6: GUI版は `IsBranchExempt` に「公務部」「本店」も追加。コンソール版は手元で修正済み。

---

## 7. 口座選択

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 1件時の自動選択 | ✓（情報表示のみ） | なし（ListViewで手動クリック） | 🔶 |
| 複数件の番号選択 | ✓ | ListView クリック | 🔶 |
| 差押実績マーク表示 | ✓（★ + 文書番号） | ✓（★ + ToolTip に執行日・文書番号） | 🔶 GUI版の方が情報量多い |
| 差押実績ルックアップ構築 | `Import-Csv` → ハッシュテーブル | `ReadAllLines` + ParseCsvLine → Dictionary | ⚠️ |
| 差押実績のキー | 宛名番号 + "\|" + 口座番号 | 宛名番号 + "\_" + 口座番号 | ⚠️ 区切り文字が異なるが機能同等 |

---

## 8. 執行日入力

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 入力形式 | 7桁和暦のみ（例: 5080615） | 7桁和暦 + 8桁西暦 + yyyy/MM/dd + yyyy/M/d | 🔶 GUI版の方が柔軟 |
| カレンダーUI | なし | Calendar Popup | 🔶 |
| バリデーション | 5段階（形式→元号→年→月→日） | ParseFlexibleDate で一括パース（不正値は赤枠） | ⚠️ |
| 過去日チェック | ✓（警告、続行可能） | なし | 🔶 |
| 遠い未来チェック | ✓（60日以上先で警告） | なし | 🔶 |
| 和暦→7桁変換 | 不要（直接入力） | DateToWareki で変換 | ⚠️ |

---

## 9. 届出住所

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 出力有無の選択 | 対話的に Y/N | CheckBox | 🔶 |
| 50文字制限 | ✓（超過時は再入力ループ） | ✓（リアルタイムバリデーション + エラー表示） | ⚠️ |
| 表紙ブロック切替時の自動更新 | なし（1回のみ表示） | ✓（口座選択時にCoverIndexが変わると自動更新） | 🔶 GUI版が優位 |

---

## 10. 確認・確定

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 確認画面 | テキスト一覧表示 | フォーム上に常時表示 | 🔶 |
| 確定操作 | Y（追加）/ N（スキップ）/ R（やり直し） | 「一覧に追加」/「スキップ」ボタン | 🔶 |
| やり直し機能 | ✓（同一ファイルのシート選択に戻る） | なし（再読み込みボタンで代替可能） | 🔶 |

---

## 11. 差押文言の生成

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| Line1 構成 | ✓ | ✓ | ✅ |
| ゆうちょ「記号番号」形式 | ✓ | ✓ | ✅ |
| その他「口座番号」形式 | ✓ | ✓ | ✅ |
| 全角変換（口座番号・支店番号） | ✓ | ✓ | ✅ |
| 口座種別補完（global + 金融機関別） | ✓ | ✓ | ✅ |
| ワイルドカード前方一致 | ✓（キー末尾「*」） | ✓（キー末尾「*」） | ✅ |
| 振込手数料（feeMapping） | ✓ | ✓ | ✅ |
| 手数料フォールバック手入力 | ✓（標準入力で対話的入力） | なし（マッピング未該当時は空） | 🔶 |
| 手数料あり時の「と手数料の合計額」挿入 | ✓ | ✓ | ✅ |
| Line2（手数料文言） | ✓ | ✓ | ✅ |
| Line3（残高・差押額テンプレ） | ✓ | ✓ | ✅ |
| CSV支店名マッピング（branchNameMapping） | ✓ | ✓ | ✅ |

---

## 12. 文書番号の採番

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| 排他ロック（FileShare.None） | ✓ | ✓ | ✅ |
| リトライ（5回・500ms間隔） | ✓ | ✓ | ✅ |
| カウンターファイル形式（nextNumber） | ✓ | ✓ | ✅ |
| CSV書込失敗時のロールバック | ✓ | ✓ | ✅ |

---

## 13. CSV出力

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| ファイル名 | 預金差押予定一覧.csv | 預金差押予定一覧.csv | ✅ |
| エンコーディング | BOM付きUTF-8 | BOM付きUTF-8（`new UTF8Encoding(true)`） | ✅ |
| ヘッダー自動生成 | ✓（ファイル未存在時に書込） | ✓（ファイル未存在時に書込） | ✅ |
| 追記方式 | ✓ | ✓ | ✅ |
| 排他ロック | ✓ | ✓ | ✅ |
| リトライ（5回・500ms） | ✓ | ✓ | ✅ |
| RFC 4180 エスケープ | ✓ | ✓ | ✅ |
| 20列構成 | ✓ | ✓ | ✅ |
| CSV列ヘッダー文字列 | 完全一致 | 完全一致 | ✅ |

---

## 14. 照会結果ファイルの保存

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| SaveAs（xlOpenXMLWorkbookMacroEnabled = 52） | ✓ | ✓ | ✅ |
| MAX_PATH チェック（260文字） | ✓ | ✓ | ✅ |
| ブック保護の解除 | ✓（SaveAs前に1回） | ✓（SaveAs前後で2回試行） | ⚠️ GUI版の方が堅牢 |
| 選択シート以外の削除 | ✓ | ✓ | ✅ |
| VeryHidden シートの保持 | `Visible -ne -2`（定数バグで意図と異なる動作） | `Visible != 2`（正しい定数値で保持） | ⚠️ ※7 |
| 選択表紙ブロック以外の行削除 | ✓ | ✓ | ✅ |
| 明細ページ削除（detailAccountNumberCell） | ✓ | ✓ | ✅ |
| 明細ページ削除（detailAccountTypeCell 併用） | ✓ | ✓ | ✅ |
| 剰余演算による動的ページ境界算出 | ✓ | ✓ | ✅ |
| 行削除の後方→前方順序 | ✓ | ✓（deleteRanges をソート） | ✅ |
| 表示位置 A1 リセット | ✓ | ✓ | ✅ |

> ※7: コンソール版の `$xlSheetVeryHidden = -2` は Excel 実定数（`2`）と異なるため、VeryHidden シートが削除対象に含まれている。Excel の内部制約で削除が静かに失敗するため実害は出ていないが、意図通りの動作ではない。GUI版は正しい定数値を使用。

---

## 15. エラーハンドリング・ログ

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| トランスクリプトログ出力 | ✓（logs/ フォルダ） | なし | 🔶 |
| 正常終了時のリネーム（_完了） | ✓ | なし | 🔶 |
| 処理完了サマリー | ✓（追加件数・スキップ理由一覧） | なし（1件ずつ結果カードで即時表示） | 🔶 |

---

## 16. 設定ファイル

| 項目 | コンソール版 | GUI版 | 状態 |
|------|------------|-------|------|
| config.json | ✓ | ✓ | ✅ |
| era_mapping.json | なし（元号は関数内ハードコード） | ✓（外部JSON、必須） | 🔶 GUI版が柔軟 |
| document_number_counter.json | ✓ | ✓ | ✅ |
| transfer_fee_mapping.json | ✓（任意） | ✓（任意） | ✅ |
| yucho_center_mapping.json | ✓（任意） | ✓（任意） | ✅ |
| account_type_mapping.json | ✓（任意） | ✓（任意） | ✅ |
| institution_name_mapping.json | ✓（任意） | ✓（任意） | ✅ |
| branch_name_mapping.json | ✓（任意） | ✓（任意） | ✅ |
| defaultFolder（GUI版のみ） | なし | ✓（任意） | 🔶 |

---

## 17. 画面遷移アニメーション（GUI版固有）

| 遷移 | 方式 |
|------|------|
| 初期画面 → ローディング | アニメーションなし（即時切替） |
| ローディング → メインフォーム | FadeOutOverlay（150ms） |
| メインフォーム → ローディング（シート切替） | FadeInOverlay / FadeOutOverlay（150ms） |
| ローディング → 結果カード | CrossFadeToResult（100ms×2） |
| 結果カード → 次ファイルのローディング | FadeOutOverlay → FadeInOverlay（150ms） |
| 結果カード（完了）→ 初期画面 | FadeOutOverlay → mainPanel FadeOut → initialPanel FadeIn（150ms×3段） |
| スキップ → 結果カード | FadeInOverlay（150ms） |

---

## コンソール版の要修正事項

| 項目 | 内容 |
|------|------|
| `$xlSheetVeryHidden` | `-2` → `2` に修正（Excel 実定数との乖離） |
| `Wait-ForFileUnlock` | 読取専用オープン前に配置されており実質不要。削除または配置見直し |
| `Test-FileLocked` | `ReadWrite` + `FileShare.None` で過度に厳格なテスト。実際の操作は読取専用 |
